from django.urls import path

from . import views

app_name ='diary'

urlpatterns =[
    # path('list/', views.diary_list, name = 'diary_list'),
    # path('detail/<int:pk>/', views.diary_detail, name = 'diary_detail'),
    # path('creat/', views.DiaryCreateView.as_view(), name = 'diary_create'),
    # path('update/<int:pk>/', views.DiaryUpdateView.as_view(), name = 'diary_update'),
    # path('delete/<int:pk>/', views.DiaryDeleteView.as_view(), name = 'diary_delete'),
    # path('like/', views.like, name='like'),
    # path('translate/', views.translate, name='translate'),
    # path('furigana/', views.furigana, name='furigana'),
]
